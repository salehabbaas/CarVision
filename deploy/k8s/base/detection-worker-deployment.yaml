apiVersion: apps/v1
kind: Deployment
metadata:
  name: carvision-detection-worker
  namespace: carvision
spec:
  replicas: 2
  selector:
    matchLabels:
      app: carvision-detection-worker
  template:
    metadata:
      labels:
        app: carvision-detection-worker
    spec:
      containers:
        - name: detection-worker
          image: ghcr.io/salehabbaas/carvision-backend:latest
          imagePullPolicy: IfNotPresent
          command: ["python", "/app/backend/app/workers/detection_worker.py"]
          envFrom:
            - configMapRef:
                name: carvision-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: carvision-secrets
                  key: database_url
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "2000m"
              memory: "3Gi"
